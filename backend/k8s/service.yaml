apiVersion: v1
kind: Service
metadata:
  name: resilience-board-api-service
  namespace: resilience-board
spec:
  type: NodePort
  selector:
    app: resilience-board-api
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
